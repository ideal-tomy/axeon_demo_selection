# フェーズ1 完了記録

実施日：2026-09-20

## 結果

`../ideal_pf` のローカル状態を `ideal_pf_selected` へ独立して複製した。掲載28件、トップカード5件、一覧・詳細・外部デモURLは維持している。フェーズ2のデプロイ、掲載デモの選定は未実施。

## 複製基準

- 複製元コミット：`c82a5aa5c58c6f1b06792bc10018da3d4b62cffd`
- 複製時の未コミット状態：未追跡の `docs/厳選版の複製・公開・選定PLAN.md` のみ。これも複製に含めた。
- 複製ファイル：276件。コピー直後のSHA-256照合は全件一致。
- 除外：`.git`、`.vercel`、`node_modules`、`dist`。
- 新しいGitを `main` ブランチで初期化。Gitリモート・Vercel連携は未設定。
- `.env` ファイルなし。アプリの環境変数参照なし。
- 依存関係は `npm ci --no-audit --no-fund` でロックファイルどおりに導入。パッケージ更新は行っていない。

## 確認結果

| 確認 | 結果 |
|---|---|
| コピー先単独の本番ビルド | `npm run build` 成功（Vite 6.4.3） |
| スマホ幅390px・PC幅1280px | トップ5件、一覧28件をブラウザで確認 |
| 一覧から詳細 | 建設デモへ遷移・再読み込み・戻る操作を確認 |
| 検索 | 「シフト」で1件表示 |
| 全詳細の直接アクセス | 28件すべて本文表示を確認 |
| JavaScript実行エラー | 0件 |
| 複製元の最終ハッシュ | 276件すべて作業前と一致 |
| コピー内の既存ファイル差分 | READMEのみ。コピーの識別・未公開状態を明記 |

自動ブラウザ検証は既存依存のPlaywrightとEdgeを使用。画面キャプチャは保存済みだが、画像閲覧ツールの環境エラーにより目視レビューは未実施。アプリのソース・画像・設定のハッシュ一致と、ブラウザ上の件数・操作・本文表示を確認した。

リンク先のデモ本体の操作審査は対象外。既存アプリの不具合修正や紹介内容の変更も行っていない。

## 記録

- `phase1-copy-manifest.json`：複製日時・元のGit状態・276ファイルのSHA-256
- `phase1-verification/file-comparison.json`：作業後のファイル照合
- `phase1-verification/result.json`：ブラウザ検証結果
- `phase1-verification/*.png`：トップ・一覧・詳細のスマホ／PCキャプチャ
- `../scripts/verify-phase1-copy.mjs`：今回の確認手順

## 次の作業

フェーズ2として、このコピーだけを新規Vercelプロジェクトへ接続し、別URLへデプロイする。既存の `ideal_pf` とその公開先は変更しない。厳選する5〜10件の検討は、その公開確認後に開始する。

補足：全ファイルの初期追加に対するGit空白検査は、複製元から引き継いだMarkdown改行用の末尾空白・既存末尾空行を検出した。同一性を保つため既存ファイルは変更せず、今回追加・編集したファイルに絞った検査を実施した。
